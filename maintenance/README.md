# نظام إدارة البلاغات والصيانة المتكامل
# Comprehensive Maintenance & Ticketing Management System

## 📋 نظرة عامة - Overview

نظام متكامل لإدارة البلاغات، الصيانة، ومخزون الأجهزة في المدارس. تم تطويره باستخدام تقنيات حديثة مع التركيز على الأمان، الأداء، وسهولة الاستخدام.

A comprehensive system for managing tickets, maintenance requests, and device inventory in schools. Built with modern technologies focusing on security, performance, and usability.

## ✨ المميزات الرئيسية - Key Features

### 1. نظام المستخدمين والصلاحيات - User Management & Permissions

- ✅ تسجيل دخول آمن مع JWT authentication
- ✅ ثلاثة أنواع من المستخدمين:
  - **العملاء (Clients)**: رفع البلاغات ومتابعتها
  - **الفنيون (Technicians)**: استقبال ومعالجة البلاغات
  - **المديرون (Admins)**: إدارة كاملة للنظام
- ✅ تشفير كلمات المرور باستخدام bcrypt
- ✅ صلاحيات محددة لكل نوع مستخدم

### 2. نظام البلاغات - Ticketing System

- ✅ إنشاء بلاغات مع تفاصيل شاملة
- ✅ رقم مرجعي فريد لكل بلاغ (TKT-XX-XXXXX)
- ✅ حالات متعددة وسجل كامل للتعديلات
- ✅ إمكانية تعيين فني للبلاغ
- ✅ تقييم الخدمة بعد الإنجاز

### 3. إدارة الأجهزة والمخزون - Device & Inventory Management

- ✅ CRUD كامل للأجهزة
- ✅ أنواع أجهزة متعددة
- ✅ تتبع حالات الأجهزة
- ✅ تاريخ الصيانة والضمان
- ✅ فلترة وبحث متقدم

### 4. نظام الإشعارات - Notifications System

- ✅ إشعارات داخلية
- ✅ إشعارات فورية عند التحديثات
- ✅ علامات قراءة

### 5. سجل النشاطات - Activity Logging

- ✅ تسجيل جميع العمليات المهمة
- ✅ تتبع المستخدم والتاريخ
- ✅ عرض السجلات للمديرين

## 🚀 التثبيت والتشغيل - Installation & Setup

### المتطلبات - Prerequisites

- Node.js (v14 أو أحدث)
- npm (يأتي مع Node.js)

### خطوات التثبيت - Installation Steps

```bash
# 1. استنساخ المشروع
git clone https://github.com/almnar0/almeshkat25.git
cd almeshkat25

# 2. تثبيت التبعيات
npm install

# 3. تشغيل الخادم
npm start
```

الخادم سيعمل على: `http://localhost:3000`

### الحساب الافتراضي - Default Admin Account

```
البريد: admin@mishkat.edu.sa
كلمة المرور: admin123
```

**⚠️ مهم جداً:** غيّر كلمة المرور بعد أول تسجيل دخول!

## 📡 API Endpoints

### Authentication
```
POST /api/maintenance/auth/register  - تسجيل مستخدم جديد
POST /api/maintenance/auth/login     - تسجيل الدخول
GET  /api/maintenance/auth/me        - معلومات المستخدم الحالي
```

### Devices
```
GET    /api/maintenance/devices      - قائمة الأجهزة
GET    /api/maintenance/devices/:id  - تفاصيل جهاز
POST   /api/maintenance/devices      - إضافة جهاز
PATCH  /api/maintenance/devices/:id  - تحديث جهاز
DELETE /api/maintenance/devices/:id  - حذف جهاز
```

### Tickets
```
GET    /api/maintenance/tickets          - قائمة البلاغات
GET    /api/maintenance/tickets/:id      - تفاصيل بلاغ
POST   /api/maintenance/tickets          - إنشاء بلاغ جديد
PATCH  /api/maintenance/tickets/:id      - تحديث بلاغ
POST   /api/maintenance/tickets/:id/assign - تعيين فني
```

## 🔐 الأمان - Security

### الإجراءات المُنفذة

- ✅ تشفير كلمات المرور (bcrypt)
- ✅ JWT Authentication
- ✅ Rate Limiting
- ✅ Input Validation
- ✅ XSS Prevention
- ✅ Role-Based Access Control

### توصيات للإنتاج

1. استخدام HTTPS
2. قاعدة بيانات حقيقية (PostgreSQL/MySQL)
3. متغيرات بيئية آمنة
4. نسخ احتياطي دوري

## 📁 هيكل الملفات - Project Structure

```
maintenance/
├── api-client.js           # عميل API
├── api-routes.js           # مسارات API
├── models.js               # نماذج البيانات
├── database-schema.sql     # مخطط قاعدة البيانات
├── devices-page.html       # صفحة إدارة الأجهزة
├── login.html              # تسجيل الدخول
├── register.html           # التسجيل
└── dashboards...           # لوحات التحكم
```

## 🎨 الواجهات - User Interfaces

### 1. لوحة العميل
- عرض البلاغات
- إنشاء بلاغات جديدة
- متابعة الحالة
- تقييم الخدمة

### 2. لوحة الفني
- البلاغات الموكلة
- تحديث الحالات
- إضافة ملاحظات

### 3. لوحة المدير
- إحصائيات شاملة
- إدارة المستخدمين
- **إدارة الأجهزة** ✅
- إدارة البلاغات
- سجل النشاطات

## 📊 قاعدة البيانات - Database

### النظام الحالي
File-based JSON storage للتطوير والاختبار.

### الترقية إلى PostgreSQL
```bash
createdb mishkat_maintenance
psql -d mishkat_maintenance -f maintenance/database-schema.sql
```

## 📞 الدعم - Support

- **المشروع**: [almeshkat25](https://github.com/almnar0/almeshkat25)
- **البريد**: admin@mishkat.edu.sa

---

**⚠️ تنويه**: هذا نظام تعليمي/تجريبي. للاستخدام في بيئة الإنتاج، يُرجى اتباع جميع ممارسات الأمان الموصى بها.
